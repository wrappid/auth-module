export const RoutesRegistry = {
  check: {
    Page        : { appComponent: "CheckUserExist", layout: "AuthLayout" },
    authRequired: false,
    entityRef   : "check",
    url         : "check"
  }
};